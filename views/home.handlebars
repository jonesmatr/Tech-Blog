<h2>All Posts</h2>
<ul>
    {{#each posts}}
    <li>
        <a href="/post/{{this.id}}">{{this.title}}</a>
        <p>{{this.content}}</p>
        <small>Posted on: {{format_date this.date_created}}</small>
    </li>
    {{/each}}
</ul>
